const findNextPower = (val,pow_) =>  Math.pow(Math.ceil(Math.pow(val + 1, 1 / pow_)), pow_)


//  const findNextPower = (val,pow_) => { return Math.pow(Math.ceil(Math.pow(val + 1, 1 / pow_)), pow_) }